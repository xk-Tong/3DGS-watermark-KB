# 3DGS_KB 设计决策与实施路线

> grill-me 会话产出，2026-07-14/15。本文档是需求拷问后的决策快照，供构建时随时回查。

## 一、项目定位

个人知识库 / Wiki，追踪 3D Gaussian Splatting (3DGS) 水印及 IP 保护方向论文。
- **主要自用**，偶尔展示给导师/同门
- **同等重要的目标**：借做这个项目学习全栈 web 开发（前后端、数据库、部署上线）

## 二、技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | Vue 3 + Vite | 用户有少量 Vue 基础；单文件组件对新手友好 |
| 后端 | FastAPI (Python) | 与 AI 流水线共用一门语言；用户已有 Python 技能 |
| 数据库 | SQLite | 单人规模足够；零配置、单文件、备份即 cp |
| 部署 | 国内轻量云服务器 2核2G 3M带宽 | ~60-99 RMB/年；国内访问快；能完整学部署流程 |
| LLM | DeepSeek | 国内可访问、便宜、抽取质量够用 |
| 访问方式 | 初期 IP+端口，免备案 | 后期再考虑域名+备案 |

## 三、数据模型

### Paper 实体

```
身份:    id, arxiv_id(可空唯一), doi(可空), title, authors(字符串列表),
         venue, pub_date, source(arxiv_auto/manual/imported), added_at
内容:    abstract, pdf_url, method_summary(AI), key_contributions(AI列表)
分类:    task_type(多选), attribute_selection, distribution_strategy,
         injection_pipeline, robustness_targets(多选), tags(自由多对多)
技术:    capacity, datasets_used(列表), baselines_compared(列表),
         psnr, ssim, bit_accuracy(三固定列,可空), extra_metrics(JSON), code_url
个人:    read_status(unread/reading/read), personal_notes,
         curation_status(auto/reviewed/verified), last_reviewed_at
```

### 分类体系（基于综述三层框架，混合范式）

**任务类型**（多选受控词表，综述第二层）：
`watermarking / steganography / tamper_localization / editing_protection / other`

**机制三维度**（单选，综述第一层，正交）：
- `attribute_selection`: sh_only / mixed / auxiliary / other
- `distribution_strategy`: global / local_frequency / local_uncertainty / other
- `injection_pipeline`: per_asset_finetune / generalizable_mapping / generation_embedded / other

**鲁棒性目标**（多选受控词表，综述第三层）：
- 2D: geometric_transform / photometric / signal_degradation
- 3D: pruning / cloning / spatial_transform / noise_injection / quantization / parameter_merging

**tags**：完全自由，承接细粒度（如 frequency-domain、fingerprinting、具体数据集名）。
每个枚举带 `other` 兜底；新方法先入 tags，积累 3-5 篇再提升为枚举值。

**KB 覆盖范围**：综述四类任务全部收录，水印设为默认筛选。
**实验对比**：PSNR/SSIM/bit_accuracy 三固定列 + extra_metrics JSON 兜底。
**关系建模**（PaperRelation cites/extends/improves/reproduces）：第一版不做，流程跑顺再加。

## 四、AI 检索-抽取-入库流水线

```
① arXiv 拉取 → ② 相关性过滤 → ③ 去重(按arxiv_id) → ④ LLM 抽取 →
⑤ schema 校验 → ⑥ 入库(curation_status=auto) → ⑦ 异步人工复核
```

- **触发**：Web UI 手动按钮，不用 cron。日期范围默认"距上次成功抓取"，避免漏论文。
- **arXiv 源**：官方 API（`export.arxiv.org/api/query`），关键词 `"Gaussian Splatting" AND (watermark OR steganography OR tamper OR "editing protection" OR "IP protection")`，搜 cs.CV/cs.CR/cs.GR。
- **相关性过滤**：LLM 判断"是否真关于 3DGS 资产 IP 保护"，剔除假阳性（2D 水印提到 Gaussian noise、3DGS 论文 related work 提一句 watermark 等）。
- **LLM 只抽分类字段**：title/authors/abstract/pub_date/arxiv_id 由 arXiv API 直接给，不进 LLM。
- **Prompt 必须附综述 taxonomy 定义 + few-shot 示例**，否则三维度分类会乱。
- **质量门**：自动入库 + `auto` 徽章 + 统计隔离（auto 不进聚合视图）+ 异步复核（auto→reviewed→verified）。
- **更新行为**：arXiv 论文修订重抓时，只覆盖 `curation_status=auto` 的字段；reviewed/verified 的人工修正不被覆盖。
- **成本**：单篇抽取 ~¥0.01-0.05；全年新论文几十篇，LLM 成本 <¥10。

## 五、冷启动策略

1. **种子**：综述 arXiv LaTeX 源码的 `.bib` 文件 → 提取 arxiv_id/doi/title
2. **批量抓取**：有 arxiv_id 的走 `id_list=` 批量 API；只有 title 的搜 arXiv 解析 ID
3. **走同一抽取流水线**：冷启动和日常增量共用代码路径，仅输入源不同
4. **相关性过滤自动剔除**综述引用的背景文献（3DGS 原始论文、通用水印理论等）
5. **补漏检索**：冷启动后跑一次关键词搜索，日期范围=[综述截止日, 今天]
6. **非 arXiv 论文**：PDF 文本提取（PyMuPDF）兜底，或 Web UI 手动表单录入

**心理预期**：一次性灌入 30-60 篇 auto 记录，不必一次审完。用到哪篇审哪篇，半年内自然审完真正会引用的部分。

## 六、检索浏览 & 可视化

**列表页**（默认首页）：
- 筛选：task_type（默认 watermarking）、三机制维度、robustness_targets、tags、年份、read_status、curation_status
- 搜索：title + abstract + method_summary + personal_notes
- 排序：pub_date / added_at / PSNR

**详情页**：全字段卡片 + 个人批注编辑区。

**对比视图**：勾选 2-4 篇 → 横向对比表（机制维度/指标/鲁棒性并排）。

**可视化**：
- 时间线（按月/年论文数，仅 reviewed/verified）
- 机制三维度分布 + 属性选择×注入管道交叉热力图
- 攻击覆盖矩阵（论文 × robustness_targets）——直击综述 R3 痛点
- 统计仪表盘（总数、各 task_type、curation 比例、近 7 天新增）

**默认视图**：水印筛选 + reviewed/verified。auto 记录库内可见但带"⚠️ AI 抽取未核实"徽章、不进统计。

## 七、原需求里被忽略、grill 后补上的部分

1. **read_status + personal_notes**——个人 KB 的核心价值，原需求全是客观信息，缺"我读过没/我的批注"
2. **curation_status**——区分 AI 抽取 vs 人工核实，否则三个月后不敢引用库里的数据
3. **实验对比建模**——异构指标无法固定 schema，需"3 headline 列 + JSON 兜底"折中
4. **相关性过滤**——arXiv 关键词搜索假阳性多，必须有一道 LLM 判断
5. **更新时保留人工修正**——arXiv 修订重抓不能覆盖 reviewed/verified 字段
6. **冷启动**——原需求只说"定期检索新论文"，没考虑已有论文怎么批量入库
7. **部署/模型耦合**——国内服务器调 OpenAI 被墙，模型选型和部署地点必须一起定
8. **质量门设计**——"自动更新"和"可信"的根本矛盾，需分层隔离而非二选一

## 八、实施路线（分阶段）

> 给"边学边做"的节奏。每阶段产出可运行的东西，别一次性憋大招。

### Phase 0｜骨架（~1-2 周）
- 项目结构：`backend/`（FastAPI）+ `frontend/`（Vue+Vite）+ `pipeline/`（AI 脚本）
- SQLite + SQLModel/SQLAlchemy 定义 Paper 表（按上面 schema）
- FastAPI 基础 CRUD（list/get/create/update）
- Vue 跑起来，列表页调通 API 显示一条手插的论文
- **里程碑**：浏览器看到论文列表

### Phase 1｜核心 CRUD + 列表/详情（~2-3 周）
- 列表页筛选条（task_type、机制维度、年份）+ 关键词搜索 + 排序
- 详情页全字段展示
- 手动新增/编辑表单（路径 C，立刻能开始用）
- read_status / personal_notes 编辑
- **里程碑**：能手动维护一个可用的小型 KB

### Phase 2｜AI 流水线（~2-3 周）
- arXiv API 封装（关键词搜索 + id_list 批量）
- DeepSeek 集成：相关性过滤 + 结构化抽取（带 taxonomy prompt）
- schema 校验 + 错误队列
- "检索"按钮触发流水线（前端调后端，后端跑 pipeline）
- curation_status 流转
- **里程碑**：冷启动能跑通，点按钮能抓新论文

### Phase 3｜可视化 + 对比（~2-3 周）
- 时间线柱状图（ECharts/Chart.js）
- 机制分布 + 交叉热力图
- 攻击覆盖矩阵
- 论文对比视图
- 统计仪表盘
- **里程碑**：能给导师展示的图都有了

### Phase 4｜部署上线（~1 周）
- 买轻量机、SSH、装 Python/Node/Nginx
- uvicorn + systemd 守护；Nginx 反代
- 前端 `npm run build` → 静态文件丢服务器或 Nginx 直出
- SQLite 定时 cp 备份
- IP+端口访问跑通
- **里程碑**：公网可访问

### Phase 5｜冷启动实跑 + 迭代
- 跑综述 .bib 种子导入
- 跑补漏检索
- 边用边复核 auto 记录
- 视需要加 PaperRelation / 域名备案 / PDF 本地存储

## 九、关键提醒

- **先跑通再优化**：Phase 0 的列表页哪怕丑、哪怕只有一条数据，也比"设计了三周还没写代码"强。学习全栈最大的坑是过度规划。
- **每阶段都要能运行**：别把数据库设计、AI 流水线、前端全写完才联调。Phase 0 联通 list 接口那一刻，你就有了正反馈。
- **AI 抽取的 prompt 是核心资产**：taxonomy 定义 + few-shot 示例要反复迭代，单独存成可版本化的 prompt 文件。
- **SQLite 备份**：`cp kb.db kb.db.bak` 写进 cron 或手动定期做。单文件丢了一切归零。
