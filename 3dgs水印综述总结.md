这是目前第一篇系统性梳理"3D高斯泼溅（3DGS）资产知识产权保护"领域的综述论文，发表于2026年2月

论文指出当前研究存在三大缺口：

1.研究碎片化，多个IP保护任务并行，缺乏统一分类体系。

2.技术基础缺乏分析，各方法都依赖"高斯扰动"，但无人系统总结其机制。

3.鲁棒性评估不完整，生成式AI带来的新型攻击几乎无人研究。

## 核心贡献总结

论文提出了一个自底向上三层框架，统一梳理了已有方法，并：

- 分析底层高斯扰动机制（三维度）；
- 区分被动/主动保护两大范式（四类任务）；
- 识别AIGC时代的新型鲁棒性威胁；
- 提出六大未来研究方向。

论文的核心骨架是图1所示的三层结构，从底向上依次是：

```bash
第三层（顶）：鲁棒性威胁 ← AIGC时代新挑战
      ↑
第二层（中）：IP保护范式 ← 被动 & 主动保护任务
      ↑
第一层（底）：高斯扰动机制 ← 所有方法的共同技术基础
```

这个框架的逻辑是：所有保护方法都建立在对高斯基元施加扰动的基础上（第一层），扰动服务于不同的保护目标（第二层），而这些保护在真实世界面临各种攻击威胁（第三层）。

## 第一层：高斯扰动底层机制

所有3DGS IP保护方法的共同技术基础是：向高斯基元中嵌入不可见的扰动。论文沿三个维度分类：

### 维度一：属性选择机制（选哪个参数来嵌入扰动）

高斯基元包含四类参数：位置μ、协方差Σ、不透明度α、球谐系数c。选择不同参数嵌入扰动，效果截然不同。

### 1.仅扰动球谐系数

**原理：** SH系数控制颜色/外观，修改它不会影响高斯的几何形状（位置、大小、方向不变）。

**代表方法：** GuardSplat、StegaGaussian等。

**优点：**

- 几何保真度高，不影响场景结构；
- 概念清晰，实现相对简单。

**缺点：**

- 容易被检测出已被水印处理；
- 面对生成式编辑时保护能力不足——编辑模型会重新计算外观，SH扰动被抹除。

### 2.混合属性扰动

**原理：** 同时扰动SH系数和几何类参数（位置 μ、协方差 Σ、不透明度 α）中的部分或全部。

**代表方法：** GaussianMarker、AdLift、DEGauss 等。

**优点：**

- 保护能力更强，不依赖单一属性；
- 几何扰动更难被生成式模型一次性抹除。

**缺点：**

- 几何参数极为敏感——位置的微小偏移就会引起渲染质量显著下降；
- 需要更复杂的优化来平衡视觉质量和保护强度。

### 3.辅助属性

**原理：** 不修改任何原始参数，而是给每个高斯基元额外附加新的属性。

**代表方法：** GS-Hider、SecureGS 等。

**优点：**

- 原始参数完全不动，视觉质量完美保留；
- 嵌入容量大，扩展性好。

**缺点：**

- 辅助通道与渲染无关，攻击者可以直接剪裁/清除这些额外通道，而不影响渲染效果——鲁棒性最差。

### 维度二：分布策略（扰动施加在哪些高斯基元上）

### 1.全局策略

**原理：** 对所有高斯基元均匀施加扰动，不做选择。

**适用场景：** 隐写术（需要大容量）、编辑防护（需要全面覆盖）。

代表方法：AdLift、DEGauss、KeySS 等。

**优点：**

- 信息容量最大；
- 不依赖特定空间/频率区域，攻击者难以定向清除。

**缺点：**

- 需要精细优化，否则视觉质量整体下降。

### 2.局部策略

**原理：** 只对高斯基元的某个子集施加扰动，子集选择可依据频率或不确定性等先验。

**① 傅里叶频率引导：**

不同频率组件有不同的保真度/鲁棒性权衡：

- **高频成分** → 与精细外观细节紧密耦合，感知敏感，但容易被压缩、重建、生成式编辑破坏（不鲁棒）；
- **低频成分** → 编码稳定的结构/语义信息，鲁棒但容量有限。

实践中一些方法（3D-GSW、CompMarkGS）探索混合频率策略，兼顾两者。

代表方法：RDSplat、StegaGaussian。

**② 不确定性引导：**

**原理：** 在3DGS中，每个高斯基元对最终渲染的贡献程度不同——高不确定性的基元（通常位于物体边界处）对渲染质量贡献最小，对它们施加扰动引起的视觉变化最小。

代表方法：GaussianMarker、MarkSplatter。

**优点：**

- 对视觉质量影响极小；

**缺点：**

- 数量和空间分布本身就有限，因此此类策略在可实现的嵌入容量和灵活性方面常常面临限制。

### 维度三：扰动注入管道（怎么把扰动注入进去）

### 1.逐资产微调

**流程：** 对每一个3DGS资产，单独运行一次优化过程，直接修改高斯参数。

**优点：**

- 灵活，可以为每个资产定制鲁棒性-保真度权衡；
- 不需要大规模预训练数据。

**缺点：**

- 计算成本随资产数量线性增长；
- 对新资产无法泛化。

**代表方法：** GuardSplat、GS-Checker 等大多数方法。

------

### 2.可泛化映射

**流程：** 预先在大规模3DGS数据集上训练一个通用编码器，推理时对任意新资产只需一次前向传播。

**优点：**

- 推理效率极高；
- 对新场景的泛化能力强。

**缺点：**

- 复杂的扰动建模导致训练不稳定；
- 大规模3D数据集上的联合训练需要大量多GPU计算资源，初始部署成本高。

**代表方法：** GS-Marker、MarkSplatter。

------

### 3.生成内嵌入

**流程：** 将扰动直接集成进3DGS生成模型中，资产从起初就携带保护信息。

**优点：**

- 大规模内容生产效率极高；
- 鲁棒性强——扰动深度嵌入生成管道，难以事后去除。

**缺点：**

- 只能保护由该特定生成模型产出的资产，无法保护已有的历史资产。

**代表方法：** InstantSplamp、GaussianSeal。

## 第二层：IP保护范式（被动 & 主动，四类任务）

### 被动保护：事后追溯，不主动限制使用

被动保护的核心思想是"打标记等验证"——资产功能不受影响，但通过嵌入的信息可以事后证明所有权或检测篡改。

### 一：3DGS水印

**核心挑战：** 如何在保持视觉质量的同时，让水印在噪声、压缩、几何变换乃至生成式编辑后仍然存活。

### 二：3DGS隐写术

与水印不同，隐写术关注大容量秘密信息的隐蔽嵌入，与水印的区别：

- 水印：低容量、强鲁棒性优先；
- 隐写：高容量、高不可感知性优先，对主动攻击鲁棒性要求相对低。

### 三：3DGS篡改定位

检测并空间定位3DGS资产中被恶意修改的区域，用于完整性验证和取证。

目前只有**GS-Checker（AAAI'26）**。

### 主动保护：主动限制，让资产抵抗被编辑

主动式3DGS知识产权保护可主动限制3DGS资产未经授权的下游使用。当前的主动式方法主要针对编辑保护措施。

### 四：3DGS编辑防护

近年来大量文生3D编辑方法使得用自然语言指令修改3DGS场景成为可能——这也带来了身份欺骗、虚假信息等严重风险。

3DGS编辑保护任务旨在为原始3DGS资产生成一个受保护的资产，使得指令驱动的编辑要么引起最小的语义变化，要么显著降低编辑质量

![image.png](attachment:219fb871-6112-4c22-be6f-dfe7633e90ac:image.png)

## 第三层：鲁棒性威胁

### 传统2D/3D失真

**2D失真（作用于渲染图像）：**

- 几何变换：旋转、裁剪；
- 光度变换：亮度/对比度调整；
- 信号退化：高斯模糊、噪声注入、JPEG压缩。

**3D失真（直接作用于高斯参数空间）：**

| 类型   | 具体操作                                                     | 危害机制                                                    |
| ------ | ------------------------------------------------------------ | ----------------------------------------------------------- |
| 几何类 | Pruning（剪枝）、Cloning（克隆）、空间变换（旋转/缩放/平移） | 改变高斯密度分布，稀释扰动；位置/协方差错位导致几何编码失效 |
| 退化类 | 高斯噪声注入                                                 | 同时破坏几何和外观                                          |
| 压缩类 | 模型量化、参数合并                                           | 压制低幅度扰动信号                                          |

## 六大未来研究方向

### 鲁棒性维度

### R1：抵御生成式净化与编辑

同时保证高不可见性和高生成鲁棒性的嵌入策略，在生成式威胁下实现两者是当前最硬的开放问题。

------

### R2：跨表示生存

现实中3DGS资产常被转换为网格（mesh）、体素（voxel）、NeRF等其他格式，现有方法几乎无法在格式转换中幸存。

不同表示之间的信息丢失，使得嵌入信息的载体在转换后可能根本不存在。

------

### R3：统一基准

现有方法使用不同数据集、不同攻击配置、不同评估指标，相互之间无法公平比较（从Table 1可以看出各方法覆盖的攻击类型参差不齐）

为了实现系统性进展，社区迫切需要一个统一的基准测试，提供标准化的攻击套件（从常规失真到生成攻击）、固定的多样化测试场景集以及用于不可感知性和鲁棒性的统一指标。

------

### 效率维度

### E1：可泛化的通用扰动

**问题：** 大多数方法依赖逐资产微调，计算代价高，无法用于大规模资产管线。

一个重要的未来方向是开发可泛化的映射或通用的扰动，这种方法应该在大规模3D数据集上进行预训练，使得在推理时能够通过单次前向传播将扰动注入到任意3DGS资产中，而无需对每个资产进行微调。

------

### E2：重建与生成3DGS的内建安全

**问题：** 随着3D内容的来源从手工重建转向前向重建和生成式模型，后处理式的保护变得越来越被动。未来的前馈重建和大规模生成模型应纳入内置的知识产权保护能力。

------

### 保护范式维度

### P1：全生命周期保护范式

**问题：** 现有研究几乎全部聚焦于事后验证（水印/隐写），这些方法侧重于所有权识别而非使用控制。对主动限制使用的研究刚起步（只有编辑防护）。

**完整生命周期保护应包含：**

| 阶段             | 保护机制                               | 现状         |
| ---------------- | -------------------------------------- | ------------ |
| **初始不可用性** | 不可学习扰动——防止资产被用于训练下游AI | 几乎未被探索 |
| **使用限制**     | 只能在授权场景使用                     | 少量工作     |
| **使用追踪**     | 水印/隐写——事后溯源                    | 主流研究方向 |
| **删除权利**     | 数据遗忘——授权撤销后从模型中抹除影响   | 极少探索     |

