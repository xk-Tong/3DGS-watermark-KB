# 2026-07-15 工作日志

## Phase 0 完成：项目骨架搭建

执行 grill-me 产出的 Phase 0 方案，全部 8 步完成并验证通过。

### 产出
- 后端：FastAPI + SQLModel + SQLite，Paper 模型含 8 个枚举 + JSON 列存储多选/列表字段
- 后端：list/get/create 三个端点 + GuardSplat 种子数据 + Swagger 文档
- 前端：Vue 3 + Vite + Element Plus + Router + Pinia + axios，列表页 el-table 展示种子论文
- 联调验证：后端 health/list/detail 正常、前端代理拿到数据、CORS 头正确、根路由重定向正常

### 关键技术决策（执行中固化）
- 多选/列表字段全部用 `Field(default=[], sa_column=Column(sa.JSON))` JSON 列，单表存储
- DB 用 `SQLModel.metadata.create_all`，不上 Alembic（Phase 2 再上）
- Node workspace 兼容：`ln -s workspace/node_modules frontend/node_modules` 解决 Vite ESM resolve
- update 端点推迟到 Phase 1（和编辑表单一起做）
- 教学注释规范：按 code-teaching-mentor skill 加中英混合结构化注释

### 环境细节
- **Python: 本地 miniconda 环境 `3dgsw_kb`**（/opt/miniconda3/envs/3dgsw_kb/bin/python, Python 3.12.13）
  - 用户要求不用 workbuddy managed venv，改用本地 miniconda 专用环境
  - 已删除 workbuddy venv（envs/default），保留 base managed python（workbuddy 自身用）
- **Node: 本地 nvm 的 Node v24.18.0**（/Users/kang/.nvm/versions/node/v24.18.0/bin/node, npm 11.16.0）
  - 用户要求不用 workbuddy managed runtime，改用本地 nvm 环境
  - 前端依赖装在 frontend/node_modules 本地（标准 Vue 项目结构），不装 workbuddy workspace
  - 已清理 workbuddy workspace 的 vue 相关包，恢复为仅 pdf-lib/pdf-parse/sharp
- 后端端口 8000，前端端口 5173
- .gitignore 关键点：软链接 frontend/node_modules 需用不带斜杠的写法才被忽略（现已改为本地 node_modules，无软链接）

### git
- commit b38d4e9: Phase 0 skeleton, 32 files, 1781 insertions

## Phase 1 完成：核心 CRUD + 列表/详情

执行决策文档 Phase 1，全部任务完成并验证通过。

### 产出
- 后端：list 端点扩展（task_type/机制三维度/year/read_status/curation_status 筛选 + sort_by/order 排序 + 4 字段关键词搜索）
- 后端：PATCH /api/papers/{id} 部分更新端点（exclude_unset=True 实现 PATCH 语义，改 read/curation 自动填 last_reviewed_at）
- 后端：PaperUpdate schema（所有字段 Optional，支持部分更新）
- 前端：列表页重写（筛选条 + 搜索框 + 排序 + 分页 + 新增按钮）
- 前端：详情页全字段卡片（5 分组：基本/摘要/方法/分类/实验/个人）+ 编辑弹窗
- 前端：PaperEditDialog 编辑表单（4 tab 分组：基本/分类/实验/个人）
- 前端：PaperCreateDialog 新增表单（复用 4 tab 结构）
- 前端：options.js 枚举选项集中配置（value/label 映射）

### 关键技术决策
- PATCH 而非 PUT：编辑表单只改部分字段，exclude_unset=True 跳过没传的字段
- task_type 多选筛选用 LIKE '%"value"%'（JSON 数组 hack，小规模够用，不用关联表）
- el-select multiple + allow-create 模拟"可输入标签"处理字符串列表字段
- extra_metrics 用 textarea + JSON.parse 让用户手填 JSON 对象
- 深拷贝用 JSON.parse(JSON.stringify()) 避免 form 和 paper 引用共享

### 验证
- 列表筛选（task_type/attribute_selection）正常
- 关键词搜索（4 字段）正常
- PATCH 更新（read_status + personal_notes + 自动 last_reviewed_at）正常
- 创建新论文正常，测试数据已清理

### 下一步（Phase 1）
列表筛选条、详情页全字段卡片、update 端点 + 编辑表单、关键词搜索框
